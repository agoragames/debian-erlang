Setup
=====

$ vagrant up
$ vagrant ssh
$ cd erlang

Building
========
$ ./build

Releasing
=========
$ ./release

Testing
=======
$ sudo dpkg -i erlang-*deb   # may fail for missing dependencies
$ sudo apt-get -fy install    # fixes missing dependencies and finishes install
